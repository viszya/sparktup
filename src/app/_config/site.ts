import type { SiteConfig } from "@/server/types"

export const siteConfig: SiteConfig = {
  name: "Sparktup",
  description:
    "SparktUp is a web app that helps solo developers and small teams to build and grow their products.",
  url: "",
  ogImage: "",
}
