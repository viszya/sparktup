import type { SiteConfig } from "@/server/types"

export const siteConfig: SiteConfig = {
  name: "Sparktup",
  description:
    "SparktUp is a web app that helps companies connect with their applicants in the most efficient way possible.",
  url: "",
  ogImage: "",
}
